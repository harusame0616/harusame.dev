---
import { type HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"input">;
const props = Astro.props;
---

<input
  class="bg-input px-2 h-10 border-b-primary border-b rounded-sm"
  {...props}
/>
