---
import Tag from "./tag.astro";

interface Props {
	tags: {
		id: string;
		name: string;
	}[];
}
const { tags } = Astro.props;
---

<ul class="flex flex-wrap gap-1">
  {
    tags.map((tag) => (
      <li>
        <Tag {...tag} />
      </li>
    ))
  }
</ul>
